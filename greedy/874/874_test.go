package greedy

import "testing"

func Test_874(t *testing.T) {
	tests := []struct {
		name   string
		expect int
		real   int
	}{
		{"case1", 0, 0},
		{"case1", 0, 0},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			//if ...
		})
	}
}
